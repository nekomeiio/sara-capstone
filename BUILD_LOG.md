## Build Log
| Date | Time Spent | Rough Tokens Used | What Shipped |
|------|-----------|--------------------|--------------|
| 2026-09-11 | 1h | ~1.3k | Set up repo structure, architecutre added plan.md and BUILD_LOG.md |
| 2026-09-17 – 2026-09-18 | 6h | ~400k | Phases 1-6: Next.js/Prisma/SQLite scaffold; wardrobe upload + Gemini vision tagging; prompt-based outfit generator; Try Something New with dedup/fallback + worn-history logging; inspiration upload + style profile merged into prompts; history calendar, empty states, error toasts. Verified end-to-end against the live Gemini API throughout, which surfaced real issues fixed along the way: swapped retired/quota-limited Gemini models for working ones, fixed a Next.js Turbopack bug that silently broke uploaded-photo display, fixed a timezone bug in the worn-outfit date picker, and added a delete option for logged outfits. |
| 2026-09-18 | 7.5h | ~350k | Delete option for logged worn outfits (DELETE endpoint + UI) — while committing this, caught and fixed a git staging mistake that had silently dropped part of it from an earlier commit. Made calendar day numbers clickable to set the log-outfit date instead of only the manual date input. Removed the "today" highlight from the calendar. Renamed the app from "Outfit Picker" to "Style Me" across the nav, page title, and package.json, and cleaned up a stray package-lock.json name mismatch left over from an earlier local rename attempt. |
| 2026-09-23 | 1.5h | ~150k | Added the ability to delete the style profile from the Inspiration tab once all inspiration images under it are gone (new `DELETE /api/style-profile`, gated so it 409s if images still exist). Fixed wardrobe/inspiration uploads failing outright with a generic "Couldn't analyze this image" error — root cause was the vision-tagging Gemini model (`gemini-3.1-flash-lite`) returning 503 "high demand" on every attempt, and the existing retry logic only ever retried the same model. Added `gemini-3.6-flash` as an automatic fallback in `generateJson()` and stopped swallowing the underlying Gemini error so failures like this show up in server logs going forward. |
| 2026-09-24 – 2026-09-25 | ~4h | ~600k | Full visual redesign toward a gothic-lace aesthetic from reference images: new site background art, a bordered white "frame" card holding all page content on top of it, serif italic headings, glossy pill-shaped nav buttons with dagger (†) accents and a click/active rim highlight, and consolidated the repeated ad-hoc Tailwind strings across ~15 files into shared classes in `globals.css` (`.card`, `.btn-pill`, `.tile`, `.thumb`, `.muted`, etc). Split the root layout into a `(site)` route group so a standalone landing page ("Ready to be styled?" banner, floating angel-wings decal, Start button) lives outside the app chrome at `/`, moving the outfit generator to `/home`. Added a reload guard (Navigation Timing API) that sends users back to the landing page on an actual browser refresh. Along the way, hit and fixed a real Tailwind v4 bug — `@apply`-ing one custom component class inside another isn't allowed and silently 500'd every page until the button classes were flattened into standalone rules. Background art and the floating-wings decal were swapped several times per updated reference images; the wings PNG was cropped to its actual bounding box and the background's character was enlarged in-place (crop → upscale → feathered composite) rather than just zooming the whole image. *Note: this redesign work is done but not yet committed to git.* |