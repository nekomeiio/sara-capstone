# Local sqlite file used only by `prisma migrate dev`; the app connects to Turso below.
DATABASE_URL=file:./dev.db

TURSO_DATABASE_URL="libsql://<your-db>.turso.io"
TURSO_AUTH_TOKEN=""

# Only AI provider used at runtime — see plan.md Section 0
GEMINI_API_KEY=""
